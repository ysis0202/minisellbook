import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moneycells.app',
  appName: 'MoneyCells',
  webDir: 'out',
  server: {
    url: 'https://minisellbook-ouuh-ir1vd25q-jjinhyuks-projects.vercel.app',
    cleartext: true
  }
};

export default config;

